/* Je vraagt aan de gebruiker om een jaar om te zetten naar maanden. **/
var jaren = parseFloat(window.prompt("Geef aantal jaar in."));
var maanden = jaren * 12;

alert(`${jaren} jaar is ${maanden} maand(en).`);